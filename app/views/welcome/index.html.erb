<% if user_signed_in? %><br>
  <%= link_to 'Show my books', books_url%>
    <h1>Shared Books</h1>
    <% @books =  Book.where(status: true) %>
    <% if Book.where(status: true).any? %>

      <table>
        <thead>
          <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Pagecount</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @books.each do |book| %>
            <tr>
              <td><%= book.title %></td>
              <td><%= book.author %></td>
              <td><%= book.pagecount %></td>
              <td><%= link_to 'Show', book %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
      <%= link_to 'Lend Your Book', new_book_path %>

    <br>

    <% else %>

      <p>There isn't book yet. Be the first reader who can share super books. </p>
      <%= link_to 'Lend Your Book', new_book_path %>

    <% end %>

<% else %>
<h1>Welcome to Book Sharing Platform</h1>
<img itemprop="image" src="http://www.mallofantalya.com.tr/blog/content/upload/images/large/2019/06/deniz_kenarnda_kitap_okumak.jpg" alt="Deniz Kenarında Elinizden Düşmeyecek Bir Kitap: CERRAH" width="680" height="400">
<p>You can borrow or lend books from this platform.</p>
<p>You just need to <b> log in </b> or <b> sign up </b> for showing available books...</p>
<% end %>